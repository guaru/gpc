version: '3'

services:
     app:
         build:
             context: web
             dockerfile: Dockerfile
         image: gpc_app
         ports:
            - 8082:80   
     api:
         build:
             context: api
             dockerfile: Dockerfile
         image: gpc_api
         ports:
            - 8083:8080


